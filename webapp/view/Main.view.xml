<mvc:View controllerName="btpexpedientedigital.controller.Main"
    xmlns:mvc="sap.ui.core.mvc"
    xmlns:core="sap.ui.core"
    xmlns="sap.m">
    <Page id="page" title="{i18n>title}">
        <Panel
            headerText="Selección de usuarios"
            class="sapUiMediumMarginTop sapUiMediumMarginBottom"
            width="auto">
            <content>
                <Input
                    id="inptBuscarUsuario"
                    placeholder="Ingrese nombre o ID de usuario"
                    showSuggestion="true"
                    class="sapUiSmallMarginTop"
                    suggest=".onSuggest"
                    width="60%"
                    suggestionItemSelected=".onSuggestionItemSelected"
                    suggestionItems="{
                        path: 'SFSF>/User',
                        length: 10,
                        parameters: {
                            select: 'userId,firstName,lastName,title,email'
                        }
                    }">
                    <suggestionItems>
                        <core:ListItem
                            text="{SFSF>firstName} {SFSF>lastName}"
                            additionalText="{SFSF>userId}"
                        />
                    </suggestionItems>
                </Input>
                <Button
                    id="helloDialogButton"
                    text="Agregar"
                    tooltip="Agregar empleado a la tabla"
                    press=".onAgregarUsuario"
                    class="sapUiSmallMargin"
                />
            </content>
        </Panel>

        <Panel>
            <Table
                id="expedienteTable"
                width="auto"
                growingThreshold="20"
                growingScrollToLoad="true"
                items="{
                    path: 'SFSF>/cust_EmployeeFile_Request',
                    parameters: {
                        select: 'externalCode,cust_userId,cust_status,cust_progressPercent,cust_jobId,externalName'
                    }
                }">
                <headerToolbar>
                    <Toolbar>
                        <Title
                            text="Usuarios"
                            level="H2"
                        />
                        <ToolbarSpacer />

                        <Button
                            icon="sap-icon://play"
                            visible="false"
                            text="Ejecutar todo"
                            type="Emphasized"
                            press=".onExcecuteAll"
                        />

                        <Button
                            icon="sap-icon://refresh"
                            press=".onRefreshTable"
                            tooltip="Actualizar tabla"
                        />
                    </Toolbar>
                </headerToolbar>
                <columns>
                    <Column>
                        <Text text="No. Empleado" />
                    </Column>
                    <Column>
                        <Text text="Nombre" />
                    </Column>
                    <Column>
                        <Text text="Estatus" />
                    </Column>
                    <Column>
                        <Text text="Avance" />
                    </Column>
                    <Column>
                        <Text text="Acciones" />
                    </Column>
                </columns>
                <items>
                    <ColumnListItem>
                        <cells>
                            <Text text="{SFSF>externalCode}" />
                            <Text text="{= !${SFSF>externalName} || ${SFSF>externalName} === 'null' ? '' : ${SFSF>externalName} }" />
                            <ObjectStatus
                                text="{
                                    path: 'SFSF>cust_status',
                                    formatter: '.formatter.statusFormat'
                                }"
                                state="{
                                    path: 'SFSF>cust_status',
                                    formatter: '.formatter.statusColor'
                                }"
                            />
                            <HBox alignItems="Center">
                                <ObjectNumber
                                    number="{
                                        path: 'SFSF>cust_progressPercent',
                                        formatter: '.formatter.percentValue'
                                    }"
                                    unit="{
                                        path: 'SFSF>cust_progressPercent',
                                        formatter: '.formatter.percentUnit'
                                    }"
                                    state="{
                                        path: 'SFSF>cust_progressPercent',
                                        formatter: '.formatter.percentState'
                                    }"
                                />
                            </HBox>
                            <HBox>
                                <Button
                                    icon="sap-icon://play"
                                    class="sapUiTinyMarginEnd"
                                    enabled="{= ${SFSF>cust_status} === 'LISTS' }"
                                    tooltip="Generar expediente"
                                    press=".onExecutePress"
                                />

                                <Button
                                    icon="sap-icon://delete"
                                    class="sapUiTinyMarginEnd"
                                    type="Reject"
                                    enabled="{= ${SFSF>cust_status} === 'LISTS' }"
                                    tooltip="Eliminar empleado"
                                    press=".onDeletePress"
                                />

                                <Button
                                    icon="sap-icon://paper-plane"
                                    class="sapUiTinyMarginEnd"
                                    enabled="{= ${SFSF>cust_status} === 'COMPLETED' }"
                                    tooltip="Enviar expediente"
                                    press=".onEnviarExpediente"
                                />
                            </HBox>
                        </cells>
                    </ColumnListItem>
                </items>
            </Table>
        </Panel>
    </Page>
</mvc:View>